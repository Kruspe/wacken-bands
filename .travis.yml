language: python
python:
  - 3.7
install:
  - pip install pipenv
  - pipenv install
  - pipenv install --dev
script:
  - pytest
deploy:
  provider: lambda
  function_name: wackenBands
  region: eu-west-1
  runtime: python3.7
  handler_name: get_bands
  zip: src
  edge: true